# Disable pnpm's newly-published-package cooldown for reproducible CI/Docker builds:
# the lockfile is the source of truth here, and the build must not reject pinned
# versions just because they were published within the default release-age window.
minimum-release-age=0
