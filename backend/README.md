# Recruitments — Backend

FastAPI + SQLModel + Pydantic AI (OpenRouter). See the repo root `README.md` for the
full setup. Dev run:

```bash
uv sync
uv run uvicorn app.main:app --reload --port 8000
```
